environments:
  - name: add-desa-env
    endpoints:
      production:
      sandbox:
  - name: dev
    endpoints:
      production:
      sandbox:
  - name: add-uat-env
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
